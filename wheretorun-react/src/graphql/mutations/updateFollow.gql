mutation updateFollow(
  $id: UUID!
  $follow: Boolean!
) 

{
  updateFollowing(
    input: {
      id: $id
      patch: {
        follow: $follow
      }
    }
  ) {
    following {
      id
      follow
    }
  }
}
