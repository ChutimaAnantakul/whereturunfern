mutation updateFollow(
  $id: UUID!
  $status: Boolean!
) 

{
  updateFollowing(
    input: {
      id: $id
      patch: {
        status: $status
      }
    }
  ) {
    following {
      id
      status
    }
  }
}
