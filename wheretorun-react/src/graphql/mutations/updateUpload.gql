mutation updateUpload(
  $id: UUID!,
  $status: Boolean
) {
  updateUpload(
    input: {
      id: $id
      patch: {
        status: $status
      }
    }
  ) {
    upload {
      id
      status
    }
  }
}

